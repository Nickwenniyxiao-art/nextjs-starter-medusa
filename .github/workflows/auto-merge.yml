# ⚠️ DEPRECATED — 已合并至 pr-automation.yml
# 保留此文件 1 周后删除（deprecated date: 2026-03-17）
name: Auto-merge PRs

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'DEPRECATED — see pr-automation.yml'
permissions:
  contents: write
  pull-requests: write

jobs:
  enable-auto-merge:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Enable auto-merge (squash)
        env:
          GH_TOKEN: ${{ secrets.CD_PAT }}
        run: |
          echo "🔄 Enabling auto-merge for PR #${{ github.event.pull_request.number }}"
          gh pr merge ${{ github.event.pull_request.number }} \
            --repo ${{ github.repository }} \
            --auto \
            --squash
          echo "✅ Auto-merge enabled"
