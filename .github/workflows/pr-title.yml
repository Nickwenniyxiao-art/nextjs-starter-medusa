# ⚠️ DEPRECATED — 已合并至 pr-compliance-gate.yml
# 保留此文件 1 周后删除（deprecated date: 2026-03-17）
# 删除 on: 触发器防止重复运行

name: PR Title Convention

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'DEPRECATED — see pr-compliance-gate.yml'

jobs:
  pr-title:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Check PR title
        if: ${{ !startsWith(github.event.pull_request.head.ref, 'codex/') }}
        uses: deepakputhraya/action-pr-title@master
        with:
          regex: '^(feat|fix|hotfix|docs|chore|refactor|test|ci)(\(.+\))?\!?\:\s.+'
          allowed_prefixes: "feat,fix,hotfix,docs,chore,refactor,test,ci"
          prefix_case_sensitive: true
          min_length: 10
          max_length: 120
